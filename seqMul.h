/*is Used to multiply the two matrices sequentially
@param A,B are the two input matrices
@param size is the input array size
@return product array of A and B
*/
double** seqMul(double **A,double **B,int size);